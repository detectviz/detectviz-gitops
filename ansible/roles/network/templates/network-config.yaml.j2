# Detectviz Platform - Network Configuration
# Generated by Ansible for dual-network architecture
# Supports:
#   - eth0 (vmbr0): External network (Management + Application traffic)
#   - eth1 (vmbr1): Internal cluster network (Kubernetes node communication)

network:
  version: 2
  ethernets:
    # External Network - Management + Application traffic (vmbr0)
    eth0:
      mtu: {{ network_mtu }}

    # Internal Cluster Network - Kubernetes node communication (vmbr1)
    eth1:
      mtu: {{ network_mtu }}
