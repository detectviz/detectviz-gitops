# Detectviz Platform - Network Configuration
# Generated by Ansible for single-network support (Phase 1)
# Supports: Management + Kubernetes Overlay (ens18)

network:
  version: 2
  ethernets:
    # Management + Kubernetes Overlay Network - All nodes
    ens18:
      mtu: {{ network_mtu }}
