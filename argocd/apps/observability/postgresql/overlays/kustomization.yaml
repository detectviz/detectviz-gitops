apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base
  - externalsecret.yaml

# NOTE: nodeSelector and storage are configured via Helm values.yaml
# No patches needed as they are already set in values.yaml
