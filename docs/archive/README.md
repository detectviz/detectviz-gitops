# Archive

此目錄包含未使用或已棄用的應用程式和腳本。

## local-path-provisioner

`local-path-provisioner` 應用程式已被封存，因為 `topolvm` 被用作本地儲存磁區管理員。同時部署 `local-path-provisioner` 和 `topolvm` 可能會導致衝突。由於 `topolvm` 是文件中提到的解決方案，因此 `local-path-provisioner` 被視為多餘。
